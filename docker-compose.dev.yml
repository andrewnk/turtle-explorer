version: "3"

services:
  gather-turtles:
    volumes:
      - ./go:/go
    tty: true

  web-backend:
    volumes:
      - ./web/backend:/node
    tty: true

  web-frontend:
    volumes:
      - ./web/frontend:/node
    tty: true
